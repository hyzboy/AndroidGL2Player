apply plugin: 'com.android.application'

android {
    compileSdkVersion 30
    ndkVersion '22.0.7026061'
    
    defaultConfig {
        applicationId 'com.android.gl2player'
        minSdkVersion 14
        targetSdkVersion 28
    }

    buildTypes {
        release {
            minifyEnabled = false
            proguardFiles getDefaultProguardFile('proguard-android.txt'),
                          'proguard-rules.pro'
        }
    }
    buildToolsVersion '30.0.3'
}

